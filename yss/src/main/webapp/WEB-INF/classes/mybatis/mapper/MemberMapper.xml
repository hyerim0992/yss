<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yss.mapper.MemberMapper">
	<select id="loginMember" parameterType="map" resultType="com.yss.dto.MemberDTO">
		SELECT m.memberId, userId, password, name, role
		FROM member m
		LEFT OUTER JOIN memberDetail d ON m.memberId = d.memberId
		WHERE userId = #{userId} AND password = #{password}
	</select>

</mapper>